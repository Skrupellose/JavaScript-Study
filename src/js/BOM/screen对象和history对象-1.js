/**
 * window的另一个属性screen对象主要保存的是客户端显示器的信息
 *  availHeight 屏幕像素高度减去系统组件高度（只读）
 * availTop 没有被系统组件占用的屏幕的最顶端像素（只读）
 * availLeft 没有被系统组件占用的屏幕的最左侧像素（只读）
 * availWidth 屏幕像素宽度减去系统组件宽度（只读）
 * colorDepth 表示屏幕颜色的位数；多数系统是 32（只读）
 * height 屏幕像素高度
 * left 当前屏幕左边的像素距离
 * pixelDepth 屏幕的位深（只读）
 * top 当前屏幕顶端的像素距离
 * width 屏幕像素宽度
 * orientation 返回 Screen Orientation API 中屏幕的朝向
 */



/**
 * history对象标识当前窗口首次使用以来用户的导航历史记录，history是window的属性，因此每个window都有自己的
 * history对象，处于安全考虑，这个对象不会暴露用户访问过的URL，但可以通过它前进和后退
 */

/**
 * go()方法接收一个整数作为参数，正为前进，负为后退
 * 同时还有两个简写方法，back()后退，forward()前进
 * history.length可以查看有多少条记录
 */

